import React from 'react';

const Header = () => {
    return (
        <div>
            <nav>
                <NavLink to="/">Home</NavLink>
            </nav>
        </div>
    );
};

export default Header;